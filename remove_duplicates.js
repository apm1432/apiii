const mongoose = require('mongoose'); require('dotenv').config(); mongoose.connect(process.env.MONGODB_URI).then(async () => { const Question = require('./models/Question'); console.log('Finding duplicates...'); const duplicates = await Question.aggregate([{ $group: { _id: { exam: '$year_exam', qnum: '$qnum', img: '$original_image_url', text: '$text' }, uniqueIds: { $addToSet: '$_id' }, count: { $sum: 1 } } }, { $match: { count: { $gt: 1 } } }]); let totalDeleted = 0; for (let dup of duplicates) { const idsToRemove = dup.uniqueIds.slice(1); await Question.deleteMany({ _id: { $in: idsToRemove } }); totalDeleted += idsToRemove.length; } console.log('Total duplicates removed:', totalDeleted); process.exit(0); }).catch(console.error);
